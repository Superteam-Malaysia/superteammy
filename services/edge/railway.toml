[build]
builder = "NIXPACKS"
buildCommand = "echo 'edge: caddy only'"
watchPatterns = ["**"]

[deploy]
startCommand = "caddy run --config Caddyfile --adapter caddyfile"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy.healthcheck]
path = "/"
timeout = 60
